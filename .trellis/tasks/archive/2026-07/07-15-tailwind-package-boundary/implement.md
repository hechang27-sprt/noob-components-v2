# Implementation plan

1. Add UI stylesheet entry, Tailwind Vite plugin, deterministic CSS output/export, and dependencies.
2. Add admin UI dependency, clean-checkout prerequisite scripts, Vite external, and public stylesheet aggregation.
3. Add demo Tailwind Vite plugin/dependencies, local scoped stylesheet, and static utility classes replacing plain CSS rules.
4. Use pnpm to update the lockfile. Verify UI/admin/demo focused commands and browser flow from a clean UI/admin `dist` state.
