# Generic JSON-to-TS locale type generator in tooling, wired into admin

## Goal

Build a generic vite plugin (tooling/vite/json-locale-types.ts) that generates TS message types from a pointed-at locale JSON directory, replace the manual admin-locale.ts interfaces with derived types, commit the generated file.

## Requirements

- TBD

## Acceptance Criteria

- [ ] TBD

## Notes

- Keep `prd.md` focused on requirements, constraints, and acceptance criteria.
- Lightweight tasks can remain PRD-only.
- For complex tasks, add `design.md` for technical design and `implement.md` for execution planning before `task.py start`.
