{
  "name": "@noob-naive-ui/admin-vue-router",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "run-s build:deps build:vite",
    "build:deps": "pnpm --filter @noob-naive-ui/admin build",
    "build:vite": "vite build",
    "clean": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\"",
    "pretest": "pnpm --filter @noob-naive-ui/admin build",
    "test": "vitest run",
    "test:watch": "vitest",
    "pretypecheck": "pnpm --filter @noob-naive-ui/admin build",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "peerDependencies": {
    "@noob-naive-ui/admin": "workspace:*",
    "vue": "^3.5.0",
    "vue-router": "^4.5.1",
    "zod": "^4.4.3"
  }
}
