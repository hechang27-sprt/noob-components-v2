---
type: package
title: "@noob-naive-ui/i18n"
description: Shared library i18n foundation — I18nText values, the per-component Composer registry that resolves overrides from the framework-wide libraryId-keyed registry, and one-way global locale sync used by every other package.
tags: [i18n, vue-i18n, override-registry]
openwiki:
  roles: [architecture, domain]
  change_kinds: [public-api, lifecycle]
  source_paths: [packages/i18n/src/index.ts, packages/i18n/src/use-component-i18n.ts, packages/i18n/src/i18n-text.ts, packages/i18n/src/use-global-i18n-sync.ts]
  symbols: [createComponentI18n, getComponentI18n, selectComponentOverrides, emptySnapshot, i18nTextSchema, resolveI18nText, I18nText, useGlobalI18nSync]
  test_paths: [packages/i18n/tests/library-i18n-selector.test.ts, packages/i18n/tests/use-component-i18n.test.tsx, packages/i18n/tests/i18n-text.test.ts, packages/i18n/tests/use-global-i18n-sync.test.tsx]
  validation_commands: ["pnpm --filter @noob-naive-ui/i18n test", "pnpm --filter @noob-naive-ui/i18n typecheck"]
---

# `@noob-naive-ui/i18n`

The dependency foundation of the workspace (`src/index.ts` exports the whole
surface). It implements a **component-first, locale-first message contract**: each
library component ships a locale-first JSON resource
(`src/locales/<ComponentName>.json`), and hosts override individual message leaves
through the **framework-wide, libraryId-keyed override registry** provided once at
app scope — without ever touching the host global Composer's registry. The registry
schema and injection key live in `@noob-naive-ui/registry` (see
[registry package](registry.md)); this package supplies the per-component Composer
machinery that reads them.

Depends only on `vue`, `vue-i18n`, `zod`, and `@noob-naive-ui/registry`.
Consumed by `@noob-naive-ui/admin`, `@noob-naive-ui/ui`,
`@noob-naive-ui/admin-vue-router` (for the persisted `I18nText` tab label), and
mirrored standalone by `@noob-naive-ui/prototype-i18n-verification`.

## Modules and responsibilities

### `i18n-text.ts` — `I18nText` and `resolveI18nText`

`I18nText` is a Zod discriminated union (`i18nTextSchema`):

```ts
type I18nText =
  | { kind: "string"; value: string }        // verbatim display text
  | { kind: "i18n"; key: string; named?: Record<string, string | number | boolean> };
```

- `i18n` values are resolved against the host global Composer **at render time**,
  so they stay reactive to locale switches and survive history-state persistence
  as their message key (admin-vue-router persists tab labels through this schema).
- `named` interpolation values must be JSON-serializable primitives because they
  are persisted with history state (enforced by the schema).
- `resolveI18nText(text, translate)` resolves an `i18n` value via the passed
  translator (e.g. `composer.t`) or returns the `string` value verbatim.

Focused tests: `tests/i18n-text.test.ts` — accepts each kind, rejects unknown
kinds and non-primitive `named` values, and proves resolution behavior.

### The shared override registry — in `@noob-naive-ui/registry`

The **schema, projections, and injection key** that used to live here
(`libraryI18nOverridesKey`, `LibraryI18nDescriptor`) moved to
`@noob-naive-ui/registry` and were generalized from i18n-only to
`{ i18n, theme }` per library. What this package still owns:

- `emptySnapshot` — the frozen `{}` every package falls back to when its registry
  entry is absent, so packaged defaults always render.
- `selectComponentOverrides(messages, componentId)` — one shared generic function
  that returns only the locales that actually carry a slice for the requested
  component, so absent locale keys never yield undefined override entries.

Focused tests: `tests/library-i18n-selector.test.ts` (3 `it`) — frozen empty
snapshot, per-component slice selection skipping absent locales, empty-tree
selection. The registry schema itself is tested in
`packages/registry/tests/library-overrides-registry.test.ts`.

### `use-component-i18n.ts` — `createComponentI18n` / `getComponentI18n`

The per-component Vue I18n registry:

- `createComponentI18n({ messages, libraryId, componentId })` creates a
  **fresh local Composer** (`useScope: "local"`, `inheritLocale: true`) that
  inherits the root locale and fallback locale, seeds packaged defaults first and
  the component's override slice second (overrides win at the leaf), then
  provides it to descendants. Each call builds a NEW Composer; it is not
  idempotent.
- The locale schema is **not passed in**: it is derived from the library's
  `LibraryOverridesRegistry` module augmentation via `libraryId`
  (`K extends RegistryI18nLibraryKey`), so each package declares its locale schema
  exactly once. `CreateComponentI18nOptions` defaults `LocaleName` and `Locale`
  from the registry entry's declared `locale` record.
- The override snapshot is resolved from the **shared registry** by `libraryId`
  (`inject(libraryOverridesKey, null)?.value?.[libraryId]?.i18n`); absent an
  entry — or a provider — the frozen `emptySnapshot` renders packaged defaults.
  The cast at consumption is the contract the registry entry's declared locale
  schema enforces.
- With `__root && inheritLocale`, Vue I18n 11.4.8 initializes fallback settings
  from the root Composer; the code then sets `composer.fallbackRoot = true` so
  keys absent from the package registry — notably host-authored tab labels and
  other host-global messages — resolve through the same local Composer.
- `getComponentI18n()` resolves the nearest ancestor component Composer and
  throws outside a `createComponentI18n` provider (fail-fast, mirroring
  `useAdminShell`). The injection key `componentI18nKey` is **module-private** —
  descendants reach the composer only through `getComponentI18n`.

Focused tests: `tests/use-component-i18n.test.tsx` (happy-dom, 10 `it`) — renders
packaged defaults without any registry entry, override-merge with sibling survival,
`fallbackRoot` kept true, host-authored key resolution, locale-ref following the
root locale, nearest ancestor resolution, the no-ancestor throw, and **compile-time
type tests** (unknown component id rejected, every packaged locale required, and a
preseeded naive-ui call rejected for invalid ids/incomplete messages). Overrides
are supplied in the harness by module-augmenting `LibraryOverridesRegistry` with a
`"test-library"` entry and `app.provide(libraryOverridesKey, ...)`, mirroring what
the admin `AdminUiConfigProvider`/`AdminConfigProvider` do ([Root Provider](admin/provider.md)).

### `use-global-i18n-sync.ts` — `useGlobalI18nSync`

One-way synchronization of a reactive locale source (ref/computed/getter) into the
host's single global Composer:

```ts
useGlobalI18nSync(() => preferences.locale); // immediate by default
```

- Intended for store-owned locale authorities: the store remains the source of
  truth and the global Composer (and every inheriting local Composer) follows.
- `immediate` defaults to `true` so the current value is authoritative at setup.
- Seeding the Composer for pre-shell screens (login page) is the host's or
  `AdminProvider`'s job (the provider seeds locale and messages at setup — see
  [Admin root provider](admin/provider.md)); this composable covers everything
  after the shell mounts.

Focused tests: `tests/use-global-i18n-sync.test.tsx` — immediate push, one-way
propagation, deferred initial write.

## The locale-first resource contract

Every translating library component owns a file `src/locales/<ComponentName>.json`
whose top level is keyed by locale (`en`, `zh-CN`, …). The admin package uses
`src/locales/AdminShell.json` and `src/locales/AdminLoginPage.json`; see
[Admin i18n](admin/i18n.md). Message-shape types are generated from
these files by `tooling/vite/json-locale-types` (see
[Tooling — Vite plugins](../tooling/vite-plugins.md)), so packaged messages and
the public override contract cannot drift.

## Change surface for package authors

To add a translating component to a library package:

1. Create `src/locales/<ComponentName>.json` (locale-first) in that package.
2. Declare the package's full locale schema (and theme schema, if any) via module
   augmentation of `LibraryOverridesRegistry` in `@noob-naive-ui/registry` — the
   schema is derived from that one declaration, never declared a second time
   ([registry package](registry.md)).
3. In the component, call `createComponentI18n({ messages, libraryId, componentId })`
   and read text through the returned composer `t`; descendants use
   `getComponentI18n()`.
4. Let `createJsonLocaleTypesPlugin` (package build) or the watcher plugin (app
   dev server) regenerate the committed `locale-types.generated.ts`.
5. The narrowest validation is that package's vitest suite (e.g. admin's
   `tests/i18n-contract.test.tsx` proves override snapshotting and zh-CN defaults)
   plus `pnpm --filter @noob-naive-ui/registry test` after a registry-schema change.

## Related

- [registry package](registry.md) — the schema, projections, and `libraryOverridesKey`
  this package consumes
- [Admin i18n](admin/i18n.md) — the admin package's registry declaration and
  override path
- [ui package](ui.md) — the ui package's (currently empty) locale schema
- [Admin root provider](admin/provider.md) — how hosts supply the registry through
  `AdminProvider`
- [Repository Overview](../architecture/overview.md) — dependency direction
