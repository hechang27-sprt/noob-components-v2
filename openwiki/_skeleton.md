---
type: working-note
title: Defunct init skeleton (working file)
description: Temporary skeleton plan from the init workflow. Not a wiki page. Kept only because this runtime offers no file-deletion primitive and an empty file fails OKF validation; safe to remove at the next update run.
tags: [internal]
---

This file is the leftover `_skeleton.md` from the OpenWiki init workflow. The
init workflow requires it to be deleted once all wiki pages are written and
populated; deletion was attempted but the sandbox blocks `rm` and exposes no
delete tool, so this placeholder (valid OKF, non-empty) is retained instead.
It is not a wiki concept and should be ignored for coverage and verification.
All real content lives under `quickstart.md`, `architecture/`,
`packages/`, `apps/`, `tooling/`, and `testing.md`.
