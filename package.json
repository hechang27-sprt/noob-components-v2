{
  "name": "noob-components-v2",
  "private": true,
  "packageManager": "pnpm@11.5.0",
  "scripts": {
    "build": "pnpm -r --if-present build",
    "typecheck": "pnpm -r --if-present typecheck",
    "lint": "pnpm -r --if-present lint",
    "format": "prettier --write .",
    "dev": "pnpm --filter admin-starter dev"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "naive-ui": "^2.44.1",
    "prettier": "^3.9.4",
    "typescript": "^6.0.3",
    "vite": "^8.1.3",
    "vue": "^3.5.39"
  }
}
